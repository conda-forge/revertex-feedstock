{% set name = "revertex" %}
{% set version = "0.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/revertex-{{ version }}.tar.gz
  sha256: b042324fa2625b900579968c77d5af24f6c88dd4be397806e0d5a61db916d574

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  entry_points:
    - revertex = revertex.cli:cli

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=77
    - setuptools-scm >=7
    - pip
  run:
    - legend-lh5io >=0.2
    - python >={{ python_min }}
    - pyg4ometry
    - hist
    - scipy
    - numpy
    - awkward
    - legend-pydataobj >=2.0.1
    - legend-pygeom-hpges >=0.9.0
    - legend-pygeom-tools >=0.0.25
    - reboost
    - dbetto
    - matplotlib-base

test:
  imports:
    - revertex
  commands:
    - pip check
    - revertex -h
  requires:
    - python {{ python_min }}
    - pip

about:
  summary: Generate Geant4 event vertices in Python
  home: https://github.com/legend-exp/revertex
  license: GPL-3.0-only
  license_file: LICENSE
  doc_url: https://revertex.readthedocs.io
  dev_url: https://github.com/legend-exp/revertex

extra:
  recipe-maintainers:
    - gipert
    - ManuelHu
